# ========== DATABASE CONFIG (Cloud + Local Support) ==========
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/student_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Kiruthika@2005}

# ========== JPA ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ========== SERVER ==========
server.port=8080